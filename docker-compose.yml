services:
  frontend:
    image: ghcr.io/bayarovemil/millennium-falcon-ui:latest
    container_name: frontend
    restart: unless-stopped
    networks:
      - app-network
    ports:
      - "127.0.0.1:3000:80"

networks:
  app-network:
    external: true